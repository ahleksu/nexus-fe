import {Button} from "@/components/ui/button";

export default function Home() {
  return (
    <div className="h-screen bg-amber-400 container m-auto flex items-center justify-center">
        <Button variant="default">Agent Home</Button>
    </div>
  );
}
